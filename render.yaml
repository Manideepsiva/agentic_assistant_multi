# One-click deployment config for Render (https://render.com)
services:
  - type: web
    name: agentic-multimodal-assistant
    env: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: GROQ_API_KEY
        sync: false        # set this secret in the Render dashboard
